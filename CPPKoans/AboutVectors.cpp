/*
 * AboutVectors.cpp
 *
 *  Created on: 14.03.2012
 *      Author: merlin
 */
#include "stdafx.h"
#include <vector>

#include "AboutVectors.h"
using namespace std;

AboutVectors::AboutVectors() {

}
/*
vector<string> vectors(){
	vector<int> v(6) ;
	v[0] = 3;
	vector<String> philosopher(4);
	philosopher[0] = "Kant";
	philosopher[1] = "Plato";
	philosopher[2] = "Hume";
	philosopher[3] = "Kierkegaard";
	return philosopher;
}
vector<double> double_vector_initialization()
{
	vector<double> doubles(1000,-1.2); // initialisiert mit -1.2
	cout << "die initialisierung " << doubles[234] << "\n";
	return doubles;
}
void resize_vectors(){

	vector<double>temps;
	double temp;
	cout << "bitte gib ein paar temperaturwerte an\n";
	while (cin >> temp){
		temps.push_back(temp); // fügt am ende einen wert an
	}
	for (unsigned int i=0;i<temps.size();++i){
		cout << temps[i] << "\n";
	}
}
// Fehler
class Bad_area{};
class Something_else{};
int area(int length, int width)
{
	if (length <= 0 || width <= 0) throw Bad_area();
	if (length == 4 || width == 0) throw Something_else();
	return length*width;
}
*/
void AboutVectors::meditate() {
}
